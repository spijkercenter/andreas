import 'package:andreas/views/scaffold.dart';
import 'package:flutter/material.dart';

class Hub extends StatelessWidget {
  @override
  Widget build(BuildContext context) => scaffold(
        Text("You are logged int"),
      );
}
